#ifndef ALGO_H
#define ALGO_H
#include <stdlib.h>

//Qui definisco alcune delle funzioni note

int find(const void*, size_t, size_t, const void *, int (*)(const void*, const void*));


#endif